This is a basic pacakge containing two nodes \
a ) To Publish a Message\
b ) To recieve a Message 
Download the code file and save it in your workspace \
source the package and run the node using rosrun :) .
